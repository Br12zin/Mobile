import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 40,
    borderColor: 'gray',
    borderRadius: 4,
    borderWidth: 1,
    margin: 10,
    paddingHorizontal: 10,
    fontSize: 16,
    color: '#000',
  },
});